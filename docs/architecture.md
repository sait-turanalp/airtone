# Architecture — moved

→ The canonical architecture doc now lives at the repo root:
**[/ARCHITECTURE.md](../ARCHITECTURE.md)** (the permanent home).

This file is kept only as a pointer so existing links don't break.
